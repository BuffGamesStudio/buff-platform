// Validation-only compatibility entrypoint.
// The v2 laboratory reuses one owner-seeded room across isolated rounds so
// the production single-open-room invariant remains enabled during proof.
await import("./movie-buff-core-v8-vip-adversarial-v2.mjs");
