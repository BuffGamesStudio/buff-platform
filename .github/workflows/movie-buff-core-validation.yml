name: Retired Movie Buff core v2 source workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Retired; use movie-buff-core-v4-validation.yml"
