name: Retired Movie Buff core v2 Windows workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: windows-2025
    steps:
      - run: echo "Retired; use movie-buff-core-v4-validation.yml"
